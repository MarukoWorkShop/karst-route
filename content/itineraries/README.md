# 逐日行程

改三条路线的**时间轴**：第几天、城市、住宿、交通、餐饮、活动。

| 文件 | 对应卡片 |
|---|---|
| `r1.yaml` | 路线一（约 14 天） |
| `r2.yaml` | 路线二（约 11 天） |
| `r3.yaml` | 路线三（约 7 天） |

路线名称、价格、封面请改上一级的 `content/routes/`。

`placeId` / `stayKind` / `themes` 只能填指南里列出的英文 id。文件顶部 `src:` 是原文语言，只改那种即可。详见 [内容编辑指南](../README.md)。
