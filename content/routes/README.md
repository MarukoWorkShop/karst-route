# 路线卡片（不是每天行程）

改三条路线在首页上的**卡片**：名称、标语、价格、封面、费用包含。

| 文件 | 路线 |
|---|---|
| `r1.yaml` | 路线一 |
| `r2.yaml` | 路线二 |
| `r3.yaml` | 路线三 |

第几天去哪、住哪，请改上一级的 `content/itineraries/`。

填写规则见 [内容编辑指南](../README.md)。文件顶部 `src:` 是原文语言，只改那种即可。
