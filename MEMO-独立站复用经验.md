# MEMO — 独立落地站复用经验（从南境拾遗提炼）

> 来源项目：南境拾遗 / Karst Route（`boutique-routes.guilinvillage.com`）  
> 用途：下次再做「品牌独立页 / 路线售卖落地站」时，按本文开干，少走弯路。  
> 更新日期：2026-09-06  
> 配套旧账本：成本与 COS 细账见 [MEMO.md](./MEMO.md)  
> **点击阅读（HTML）→ [MEMO-独立站复用经验.html](./MEMO-独立站复用经验.html)**

---

## 0. 一句话定义

做的是：**可双语的单页落地站**——客人看懂产品 → 留资；运营改 Notion/YAML，技术管部署与媒体；没有业务 CMS、没有支付。

成功不看「功能多」，看四件事：

1. 手机上 3 秒内知道卖什么  
2. 运营能自己改文案（不必改 React）  
3. Push 后可上线，有验收清单  
4. 国内访问图文够快、域名不裂

---

## 1. 从零开始：推荐阶段（可复用路线图）

```
① 一页纸定位     → 谁、卖什么、成功标准（3～5 条可测）
② PRD 定边界     → In / Out / 非用户；先砍再做
③ DESIGN 定气质  → 抽审美、丢版式；手机主视口
④ 信息架构       → 单页锚点 #top #tours #itinerary #plan …
⑤ 内容模型       → YAML/Notion 字段表；路径约定；src 语言
⑥ 竖切可上线     → 骨架 + Hero + 一条路线 + 询盘，先通闭环
⑦ 规则入库       → Cursor Rules / CONTENT / 部署门禁
⑧ 工作流闭环     → 改字 / 改图 / 上线 / 语言补齐
⑨ 打磨与防坑     → 自定义域名、媒体、手势、骨架
⑩ MEMO 归档       → 成本、决策、已知坑（本文 + 旧 MEMO）
```

**原则：先闭环，后丰满。** 不要一上来做完整 CMS、多语言机翻、支付。

### 1.1 开干前必须写清的 8 个问题

| # | 问题 | 本项目答案示例 |
|---|------|----------------|
| 1 | 对外品牌 vs 仓库工作名？ | 南境拾遗 / karst-route |
| 2 | 主设备？ | 手机 375px 优先 |
| 3 | 官方语言怎么做？ | 人工中英 1:1，路径 `/` 与 `/zh/`，禁止整页机翻当官站 |
| 4 | 内容谁改、改哪里？ | 运营 Notion/YAML；UI 词在 copy.ts |
| 5 | 询盘怎么到人？ | Web3Forms → 邮箱 |
| 6 | 静态站托管？ | GitHub Pages + 自定义域名 |
| 7 | 大媒体放哪？ | 视频/图 → 国内 COS；壳在 Pages |
| 8 | v1 明确不做？ | 支付、账号、整站 CMS、备案专线… |

---

## 2. 如何写 PRD（模板）

### 2.1 结构模板（直接复制改）

```markdown
# PRD：{品牌} / {英文名}

| 字段 | 内容 |
| 对外品牌 | |
| 产品工作名 / 仓库 | |
| 版本 / 日期 | |
| 形态 | 单页落地站 / 双语 / 询盘 … |
| 主视口 | 手机 375px |
| 技术 | Vite + React + TS + Tailwind；Pages |
| 后端 | 询盘第三方；无业务 CMS |

本文是实现与验收的唯一产品依据。视觉见 DESIGN；改内容见 CONTENT。

## 1. 背景与目标
（一段话）+ v1 成功标准（可勾选、可演示）

## 2. 用户 / 非用户
表格：角色、任务；明确「谁不是用户」

## 3. 范围
### In scope（带锚点 id）
### Out of scope（写死，防止范围蠕变）

## 4. 信息架构
单页从上到下栏目 + hash

## 5. 关键交互
折叠规则、底栏、语言切换、表单步骤

## 6. 内容与语言约束
原文 src、译文策略、专有名词

## 7. 验收清单
性能骨架、双语、询盘一封真邮件、主路径走通
```

### 2.2 写 PRD 的硬规则

- **成功标准必须可测**（「好看」不行；「Slow 3G 先出骨架」「提交一封询盘」可以）  
- **Out of scope 写进正文**，不是附录玩笑  
- **内容密度是产品约束**：日程默认折叠、同时只开一天、展开短 bullet  
- **语言策略写死**：官方对照 vs 机翻工具箱，不要混  
- PRD 改版本号；实现以 PRD 为准，DESIGN/CONTENT 不得偷改范围  

### 2.3 本项目踩过的 PRD 相关坑

| 坑 | 教训 |
|----|------|
| Pages 子路径 `/karst-route/` 写进 PRD 后换自定义域名 | PRD/构建要写「有自定义域名时 base=`/`」 |
| 「图片走 public」后来改 COS | PRD/CONTENT 写「路径相对、宿主可切换」 |
| 底栏两键常驻挡内容 | 交互演进要回写 DESIGN，别只口头改 |

---

## 3. 规则与约束怎么设

把「人会忘、AI 会漂」的事写成**仓库内强制规则**，不要只写在聊天里。

### 3.1 分层

| 层 | 放哪 | 管什么 |
|----|------|--------|
| 产品边界 | `PRD.md` | 做/不做、验收 |
| 视觉手势 | `DESIGN.md` | 色、热区、骨架、密度 |
| 改内容流程 | `CONTENT.md` + `content/README.md` | 员工改哪、图路径 |
| AI / 协作硬规则 | `.cursor/rules/*.mdc` | 每次对话自动带上 |
| 技术账本 | `MEMO.md` | 成本、欠费、决策 |

### 3.2 Cursor Rule 模板（内容语言类）

可复制为 `.cursor/rules/content-i18n.mdc`：

```markdown
---
description: 内容 YAML 只改原文；上线前按 src 补齐其它语言
alwaysApply: true
---

# 内容语言

- 文件顶部 `src: zh|en` 标原文；其它键是译文
- 员工可只改原文；不要强迫成对填写
- 永远不要覆盖/删除 src 指出的原文（除非用户明确改原文）
- 用户说「部署/上线/补齐语言」：先跑语言报告，只补空译文
- 原文变了、译文仍旧：按新原文重译非 src 语言
- 就地改 YAML，禁止整文件 dump 丢掉注释
```

### 3.3 建议再固化的规则（本项目事后应有）

| 规则名 | 一句话 |
|--------|--------|
| `media-host` | `destinations|tours|reviews|videos` 线上走 COS；路径仍是相对路径；新图 = public + COS 上传 + push |
| `deploy-gate` | 上线前：`content-lang-report` 全绿；自定义域名则 `PAGES_BASE=/` |
| `no-secrets-in-chat` | COS/Notion 密钥走弹窗或本地 env，不进 git、不进日志 |
| `mobile-itinerary` | 手风琴展开：标题钉在点击视口；内容向下长；勿乱 `scrollIntoView` |

### 3.4 设计约束（可复用口诀）

- 抽画册**审美**，不搬画册**版式**  
- 手机主视口：一屏一意；Hero 不做仪表盘  
- 品牌名要够大；别用默认 Inter 堆砌感（若品牌已定字体则跟品牌）  
- 卡片能不用就不用；底栏可折叠，勿长期挡内容  

---

## 4. System Prompt / 对 Agent 的指令包（可复用）

下面可直接贴进新项目的「项目说明」或 Cursor User Rules。

### 4.1 总控 System（精简版）

```text
你在做旅游/品牌单页落地站。以仓库 PRD.md 为产品唯一依据，DESIGN.md 为视觉依据，CONTENT.md 为改内容依据。

优先级：
1) 可上线闭环 > 功能堆砌
2) 手机体验 > 桌面装饰
3) 运营可改内容（YAML/Notion）> 改 JSX
4) 官方双语是人工对照；机翻不得冒充官站

硬禁止：
- 未要求时不 commit/push；不改 git config；不 force push main
- 不覆盖 YAML 里 src 原文语言
- 不把密钥写入仓库或回复正文
- 不擅自扩大 Out of scope

用户说「部署/上线」：
1. 跑语言完整性报告
2. 只补空译文
3. 确认媒体宿主（Pages vs COS）与自定义域名 base
4. 再 commit/push（若用户明确要求）

改图：相对路径不变；若前缀走 COS，必须上传桶后再上线。
```

### 4.2 专项 Prompt 卡片

**A. 开写 PRD**

```text
根据访谈写 PRD。必须含：成功标准（可测）、In/Out、非用户、锚点 IA、语言策略、验收清单。
先列出 8 个澄清问题；未答清不写实现细节。
```

**B. 接 Notion / YAML 内容**

```text
只改 content/**；遵守 src 原文规则与 keepTx 语义。
photos 写相对路径（destinations/… 或 reviews/…），不写完整 URL，不写 Notion 附件链。
slug 必须唯一。
```

**C. 上线门禁**

```text
部署检查：lang report → 断链图路径 → Pages base（自定义域名用 /）→
媒体是否需 COS → 再推 main。杂项（MEMO 草稿、xlsx、tgz）不进提交。
```

**D. 手机手势 / 细节**

```text
改交互前先复述用户预期（标题钉住、内容下展等）。
优先修跳动/挡手势/骨架白屏类问题。改完说明如何在 375 宽度自测。
```

---

## 5. 工作流与闭环

### 5.1 总览图

```
        ┌──────────────┐
        │  Notion 文案  │
        └──────┬───────┘
               │ sync / 人工
               ▼
        ┌──────────────┐     ┌─────────────────┐
        │ content/*.yaml│◄────│ public/ 本地图   │
        └──────┬───────┘     └────────┬────────┘
               │                      │ cos-sync
               ▼                      ▼
        ┌──────────────┐     ┌─────────────────┐
        │ git push main│     │ 腾讯云 COS 广州   │
        └──────┬───────┘     └────────┬────────┘
               │ Actions              │
               ▼                      │
        ┌──────────────┐              │
        │ GitHub Pages │──HTML/JS─────┤
        │  (壳 + base) │              │
        └──────┬───────┘              │
               │                      │
               └──────────┬───────────┘
                          ▼
              访客：自定义域名拼壳+图
```

### 5.2 三条日常闭环

| 闭环 | 步骤 | 完成定义 |
|------|------|----------|
| **只改字** | Notion → 同步/Push → 刷新 | 线上文案对，语言报告绿 |
| **改图** | 压图 → `public/…` → Notion 同路径 → `cos-sync-public.py` → Push | COS Referer 下 200；页上图对 |
| **改交互/壳** | 改 `src/` → 本地 375 自测 → Push | 无骨架卡死、无 base 404 |

### 5.3 部署门禁（Checklist）

```text
[ ] content-lang-report：served 语言无空译
[ ] 引用到的图片路径在 public 存在（或已在 COS）
[ ] 若用自定义域名：PAGES_BASE=/ （勿再写 /repo-name/）
[ ] COS 防盗链含：自定义域名 + github.io
[ ] 新图已上传 COS（destinations/tours/reviews/videos）
[ ] 不提交：密钥、.env.local、临时 xlsx/tgz、无关 MEMO 草稿（按需）
[ ] Push main 后 Actions 绿；强制刷新验证
```

### 5.4 协作角色（给团队一页纸）

| 角色 | 做 | 不做 |
|------|----|------|
| 内容 | Notion 字段、相对路径 | COS 密钥、改组件 |
| 出图 | 压图、文件名、放 public | 直接改 DNS |
| 技术 | 同步脚本、COS 上传、域名/防盗链、修 bug、Push | 擅自改 src 原文 |

---

## 6. 校验标准（验收 / QA）

### 6.1 产品验收（PRD 级）

| ID | 标准 |
|----|------|
| P1 | 手机首屏骨架与真页结构一致，非长时间白屏 |
| P2 | 路线可切换；折叠态可扫完日程；同时只开一天 |
| P3 | 询盘测试提交成功进邮箱 |
| P4 | EN/中文切换改 URL，文案与 `lang` 一致 |
| P5 | Hero + 路线卡能读出气质与差异 |

### 6.2 工程验收

| ID | 标准 |
|----|------|
| E1 | `asset()` 统一拼 URL；禁止裸绝对路径写死错 host |
| E2 | 自定义域名下 JS/CSS 为 `/assets/…` 且 200（不是 `/repo/assets` 404） |
| E3 | 走 COS 的资源：带站 Referer 为 200，外域可 403 |
| E4 | 语言报告无空键；src 原文未被机器人改写 |
| E5 | 手机展开日行程：标题约停在点击处，内容向下 |

### 6.3 内容验收

| ID | 标准 |
|----|------|
| C1 | photos 相对路径；评价不用 destinations 图床混用 |
| C2 | review slug 唯一 |
| C3 | 专有名词不乱译 |
| C4 | 单张列表/详情图建议长边 ≤1280、约 ≤250KB（国内也省 COS 流量） |

### 6.4 「像人用」验收（细节）

- 底栏不长期霸屏；可折叠或 Hero 时隐藏  
- 展开手风琴不整页瞬移  
- 图慢时先怪 host（Pages 海外 vs COS），再怪体积  
- 骨架屏只应闪一下；若一直骨架 = JS 没加载（先查 base）  

---

## 7. 打磨细节：本项目方法论

### 7.1 先定位层，再动手

```
现象 → 是壳(HTML/JS) / 是内容(YAML) / 是媒体(COS|Pages) / 是交互(滚动)
```

例：

| 现象 | 层 | 真因 |
|------|----|------|
| 一直骨架 | 壳 | 自定义域名仍用 `/karst-route/` base |
| 只有图慢 | 媒体 | 大图走 GitHub 海外 |
| 字也出不来 | 壳 | 同上，JS 404 |
| 展开乱跳 | 交互 | 手风琴高度变化未钉视口 |
| Notion 改了译没清 | 内容规则 | keepTx 策略 |

### 7.2 媒体策略（可复用决策树）

```
需要国内快？
  ├─ 是 → 图/视频进 COS（相对路径不变）
  │        └─ 防盗链加所有线上域名
  └─ 否 → 可先 public + Pages（简单，国内慢）

新图上线 = 压图 + public 备份 + COS 上传 + YAML 路径 + Push
```

### 7.3 自定义域名必做两步

1. DNS CNAME → `user.github.io`  
2. 仓库 Pages 填域名 + **用 `PAGES_BASE=/` 重新部署**  
3. COS Referer 白名单加上新域名（否则视频/图 403）  

### 7.4 打磨顺序建议

1. 通：能打开、能切路线、能留资  
2. 对：文案/图路径/语言  
3. 快：压图、COS、关测试期大视频  
4. 顺：底栏、手风琴锚点、骨架  
5. 省：流量账、CDN 阈值、关闲置云产品  

---

## 8. 可复制「新项目启动包」清单

新建类似站点时，第一周内备齐：

```text
[ ] PRD.md（含 Out of scope + 验收）
[ ] DESIGN.md（主视口、色板、密度）
[ ] CONTENT.md + content/README.md
[ ] content/langs.yaml + src 原文约定
[ ] .cursor/rules/content-i18n.mdc
[ ] 询盘通道（Web3Forms 或同类）
[ ] Pages 工作流 + PAGES_BASE 策略
[ ] public/ 目录约定 +（可选）COS 桶与防盗链
[ ] scripts：语言报告、Notion 同步、COS 上传
[ ] MEMO：域名、桶名、单价、已知坑
```

### 8.1 最小竖切（第 1 个可演示日）

1. 骨架 HTML + Hero 一屏  
2. 一条路线折叠日程  
3. 一个询盘成功  
4. Push 自动发布  
5. 再叠加双语 / 多路线 / COS  

---

## 9. System 级「解析规则」（给人和 AI）

遇到需求时按序解析：

1. **意图**：改内容 / 改壳 / 改媒体 / 上线 / 要文档？  
2. **宿主**：这句话影响 Pages、COS、Notion、DNS 哪一段？  
3. **语言**：是否触及 src 原文？部署要不要 lang report？  
4. **副作用**：改 base、防盗链、路径是否会让旧链接 404？  
5. **完成定义**：用户如何在手机上认为「好了」？  

禁止：

- 未确认就「顺便」重构  
- 用整文件 YAML dump  
- 只 push 图不上 COS（在 COS 模式下）  
- 把聊天里的临时决定不写回 PRD/CONTENT/Rule  

---

## 10. 成本测算框架（复用）

| 项 | 怎么估 |
|----|--------|
| Pages / 域名解析 | 通常 ≈ 0 |
| COS 存储 | 容量 GB × 单价（图+视频往往可忽略） |
| COS 流量 | GB × ¥0.5–0.8（大头）；视频全开时开发期也费钱 |
| CDN | 月流量常 >100GB 再挂；同流量约降至 1/3～1/4 |
| 闲置云套餐 | 定期查；与「放张图」无关的固定费可退 |

**省钱杠杆：** 压图、详情图长边限制、Hero 视频可关、防盗链拒空 Referer、少在本地代理反复打满视频。

---

## 11. 本项目「经验金句」

1. **路径是契约，宿主可换**——Notion 只写 `destinations/a.jpg`。  
2. **骨架一直转 = JS 没到**——先查 base，再查网速。  
3. **国内慢先换线路再死压 JPEG**——Pages 壳 + COS 图。  
4. **规则写进仓库，别写进记忆**——src、部署、防盗链。  
5. **展开跟手，标题钉住**——高度变化必须补滚动。  
6. **先竖切闭环，再长成画册**——PRD 的 Out of scope 是朋友。  

---

## 12. 附录：与本仓库文件的映射

| 本文概念 | 本仓库落点 |
|----------|------------|
| 产品依据 | `PRD.md` |
| 视觉手势 | `DESIGN.md` |
| 改内容 | `CONTENT.md`、`content/README.md` |
| 语言规则 | `.cursor/rules/content-i18n.mdc` |
| 媒体 | `src/lib/media.ts`、`asset.ts`、`scripts/cos-sync-public.py` |
| 部署 | `.github/workflows/pages.yml` |
| 语言报告 | `scripts/content-lang-report.mjs` |
| 成本细账 | `MEMO.md` |
| 玩法一页纸（Canvas） | Cursor canvases/`boutique-routes-playbook.canvas.tsx` |

---

*下次开新坑：复制第 8 节清单 → 填第 1.1 节八问 → 用第 2 节写 PRD → 贴第 4 节 System → 按第 5～6 节跑闭环与验收。*
