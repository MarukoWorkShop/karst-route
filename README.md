# Karst Route

广西 × 越南北部私人路书站点。

```bash
npm i
npm run dev
```

推送到 GitHub `main` 后，Actions 会自动发布到 GitHub Pages。中文版路径为 `/zh/`。

静态托管下「Plan Your Route」的服务端生成不可用；询盘表单需要在仓库 Secrets 里配置 `VITE_WEB3FORMS_ACCESS_KEY`。
